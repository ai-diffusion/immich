{
  "name": "ai-chat-exporter-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "pack": "npm run build && cd dist && zip -r ../extension.zip . && cd .."
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.4.0",
    "@types/chrome": "^0.0.268"
  }
}
